#! /usr/bin/env sh

# by default, arabesque home is the directory containing this script
# change if necessary
export ARABESQUE_HOME="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# set with the path of your spark installation
export SPARK_HOME=""
